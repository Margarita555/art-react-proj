export default function AppBar() {
  //   const isLoggedIn = useSelector(getIsLoggedIn);
  return (
    <header
    //   className={s.header}
    >
      ffffffffff
      {/* <Navigation />
      {isLoggedIn ? <UserMenu /> : <AuthNav />} */}
    </header>
  );
}
